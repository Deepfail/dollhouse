// Component stub - legacy storage removed

interface SceneInterfaceProps {
  sessionId?: string;
  onClose?: () => void;
}

export function SceneInterface({ sessionId, onClose }: SceneInterfaceProps) {
  return <div className="p-4 text-center text-muted-foreground">Scene Interface disabled - legacy storage removed</div>;
}
